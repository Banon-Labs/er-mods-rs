# Thin standalone ME3-loadable shell for the `er-invasion-warp` feature crate
# (bd er-effects-rs-5es): own DllMain, own log file, individually shippable.
#
# It is designed to COEXIST with the product DLL and the other companions: a profile may list
# it, omit it, or combine it freely. It installs NO detours at all -- it arms the host seam and
# registers one recurring `CSTaskImp` game task that READS the `CSAutoInvadePoint` coordinate
# table for oracle 1 -- so it contends on nothing. When it does start hooking, every detour
# MUST go through the `er-hook` union rather than a bare `MhHook::new`, for the same reason
# er-quit-menu-dll does: two MinHook instances patching one prologue corrupt each other's
# trampolines.
[package]
name = "er-invasion-warp-dll"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
name = "er_invasion_warp_dll"
# `cdylib` is the ME3-loadable artifact; `rlib` keeps the tiny DLL shell testable on the
# host without pulling game deps into the test graph.
crate-type = ["cdylib", "rlib"]

[dependencies]
er-invasion-warp = { path = "../er-invasion-warp" }

# Tier A only (zero external deps, host-buildable): `game_module_base()` for the RVA-relative
# native calls the warp driver makes, and the fresh-per-process log writer. NOT windows-gated,
# because the log helper is compiled on the host too -- see `append_log` below.
er-game-base = { path = "../er-game-base" }

# The GFX codec, for the world-map movie edit that gives the invasion pins a red icon. Host-
# buildable on purpose: the edit itself is pure byte manipulation and is covered by offline
# tests against the extraction corpus, so only the load-time swap needs the game.
er-gfx = { path = "../er-gfx" }

[target.'cfg(windows)'.dependencies.er-hook]
# The shared MinHook union. Every detour this DLL installs MUST go through it: two MinHook
# instances patching one prologue corrupt each other's trampolines, and er_effects_rs.dll may be
# loaded in the same process.
path = "../er-hook"

# Game-thread task registration ONLY (`CSTaskImp` + `FD4TaskData`, the er-telemetry-dll
# pattern). Windows-only so the host `cargo test` of this shell never pulls the game bindings
# into its graph.
[target.'cfg(windows)'.dependencies]
eldenring = { path = "../../../fromsoftware-rs/crates/eldenring" }
fromsoftware-shared = { path = "../../../fromsoftware-rs/crates/shared" }

# Lint parity with `../fromsoftware-rs` (root `[workspace.lints]`): stock rustc + clippy lint
# set, denied. Zero violations on the shipping target
# (`cargo xwin clippy --release --target x86_64-pc-windows-msvc --all-targets`) and on the host
# build, so the deny carries no `# DEBT:` allows. The single crate-root
# `cfg_attr(not(windows), allow(dead_code, unused_imports))` is a cfg artifact of the HOST
# build -- every consumer in this shell is `DllMain` and the `#[cfg(windows)]` hooks/tasks it
# installs, so a host compile cfg's the callers out -- not shipping-target debt. See the
# comment beside it in `src/lib.rs`.
[lints]
workspace = true

# Assembles every byte-checked prologue from NAMED instructions in `build.rs` -- for Elden Ring
# and for Seamless Co-op's ersc.dll -- so no hand-typed byte can disarm a check.
# See `build-support/prologue_build.rs`.
[build-dependencies]
iced-x86 = { version = "1.21", default-features = false, features = [
  "std",
  "code_asm",
] }
