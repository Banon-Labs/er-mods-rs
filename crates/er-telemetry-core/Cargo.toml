[package]
name = "er-telemetry-core"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
name = "er_telemetry_core"

# er-telemetry-core is the telemetry subsystem lifted out of the product DLL. It
# depends ONLY on er-game-base + upstream/sibling game libs, NEVER on product
# (er-quickload) code. Product depends on THIS crate, not the reverse.
[target.'cfg(windows)'.dependencies]
er-game-base = { path = "../er-game-base", features = ["game-types"] }
eldenring = { path = "../../../fromsoftware-rs/crates/eldenring" }
fromsoftware-shared = { path = "../../../fromsoftware-rs/crates/shared" }
er-save-loader = { path = "../er-save-loader" }

[dependencies]
# Host/tool-side (non-windows) still resolves the base crate (tier A only) so the
# shared log/mem primitives + RVA table are available for offline unit work.
er-game-base = { path = "../er-game-base" }

# Lint parity with `../fromsoftware-rs` (root `[workspace.lints]`): stock rustc + clippy
# lint set, denied. This crate is at zero violations on its shipping target
# (`cargo xwin clippy --release --target x86_64-pc-windows-msvc -p er-telemetry-core --all-targets`)
# and on the host build, so the deny carries no `# DEBT:` allows.
[lints]
workspace = true
