## What changed

<!-- One or two sentences. The change, not its history. -->

## Why

<!-- What it fixes or enables. Link the bd issue or the plan rather than restating it. -->

## Evidence

<!--
What was measured, and what was not. "Not proven: <the thing>" is a pass here;
silence is not.

Two rules this body is held to, enforced by the global cupcake policy
`github_pr_body_contract` at the moment you post, not at review time:

  * NO COUNTS a reader can count for themselves -- commits, files, lines, crates,
    tests, rows. GitHub prints those at the top of this page and recomputes them on
    every push; a copy here cannot be recomputed and is stale the moment you amend.
    Addresses, versions, dates, run ids, #refs and file:line citations are fine, and
    a measurement quoted in backticks or a fenced block is fine -- that is what a
    quotation should look like anyway.
  * 2500 CHARACTERS, whole body. If it does not fit, the change is too big: split
    the PR, or move the long reasoning to the commit message, a doc under
    docs/plans/, or the issue tracker, and link it in a line.
-->

---
 Written by <agent>, authorized by @<github-username>
